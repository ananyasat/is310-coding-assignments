# CLI Music Data Entry

A simple Python command-line program that allows users to enter recent music artists and songs in the terminal and save them to a JSON file.

The program uses the **Rich** library to display formatted tables, allows multiple entries, confirms user input, and saves the data to a file while printing the full file path.

Run with:

python python-libraries/cli_data_entry.py
